# ... (위쪽 생략) ...
    steps:
    # 1. 저장소의 코드를 가상 서버로 가져옴
    - name: 저장소 체크아웃
      uses: actions/checkout@v4  # <-- v3에서 v4로 수정

    # 2. 파이썬 환경 세팅
    - name: 파이썬 설치
      uses: actions/setup-python@v5  # <-- v4에서 v5로 수정
      with:
        python-version: '3.10'
# ... (아래쪽 생략) ...
