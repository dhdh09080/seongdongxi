name: ë‹¹ì¼ ì²´ê°ì˜¨ë„ í¬ìŠ¤í„° (09ì‹œÂ·14ì‹œ)

on:
  schedule:
    # ì‹¤í™©ì€ ë§¤ì‹œ 40ë¶„ ì´í›„ ì œê³µë˜ë¯€ë¡œ 45ë¶„ì— ì‹¤í–‰ â†’ ì •ì‹œ ì‹¤í™© í™•ë³´
    - cron: '45 0 * * *'   # 09:45 KST (09ì‹œ ì‹¤í™©)
    - cron: '45 5 * * *'   # 14:45 KST (14ì‹œ ì‹¤í™©)
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: repo-commit
  cancel-in-progress: false

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: í•œê¸€ í°íŠ¸ ì„¤ì¹˜
        run: sudo apt-get update && sudo apt-get install -y fonts-nanum && sudo fc-cache -fv
      - run: npm install canvas
      - name: í¬ìŠ¤í„° ìƒì„±
        env:
          KMA_API_KEY: ${{ secrets.KMA_API_KEY }}
          GRID_NX: '61'
          GRID_NY: '127'
          POSTER_TYPE: 'daily'
        run: node scripts/generate-poster.js
      - name: ì»¤ë°‹
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add snapshots/
          if ! git diff --staged --quiet; then
            git commit -m "ðŸ“¸ ë‹¹ì¼ ìŠ¤ëƒ…ìƒ·: $(TZ=Asia/Seoul date +'%Y-%m-%d %H:%M KST')"
            git pull --rebase --autostash || true
            git push
          fi
